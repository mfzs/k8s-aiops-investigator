FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    LOG_FORMAT=json

WORKDIR /app

RUN addgroup --system investigator \
    && adduser --system --ingroup investigator investigator

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app ./app

USER investigator

CMD ["python", "-m", "app.main"]
